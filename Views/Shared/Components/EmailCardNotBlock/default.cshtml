@model LabReservation.Models.UserEmail

<link href="~/css/emailCard.css" rel="stylesheet" />

<div class="email-card">
    <div class="email-info">@Model.email</div>
    @Html.ActionLink("", "AddToBlock", "Blacklist", new {userid = @Model.user_id}, new {@class="fas fa-ban"})
    @* <div class="ban-btn"><button><i class="fas fa-ban"></i></button></div> *@


</div>